function makeResult(){var q=currentQuestion(),e=elementReading(),recent=flow.lastContact==='today'||flow.lastContact==='days3',cautious=flow.mood==='anxious'||flow.mood==='tired'||e.kind==='tension',r={};if(q.id==='love_silent'){r.verdict=recent&&!cautious?'ความเงียบครั้งนี้ยังไม่ใช่สัญญาณว่าหมดใจ':'เขายังไม่ได้ปิดความรู้สึกทั้งหมด แต่กำลังหลบแรงกดดัน';r.summary='จังหวะวันนี้ทำให้คุณต้องการคำตอบเร็วกว่าอีกฝ่าย ความเงียบจึงดูหนักกว่าความหมายจริง ควรดูความสม่ำเสมอและน้ำเสียง มากกว่าจับเวลาว่าอ่านแล้วตอบเมื่อไร';r.action=recent&&!cautious?'ส่งข้อความสั้นหนึ่งครั้ง เช่น “ช่วงนี้เป็นยังไงบ้าง ถ้าว่างค่อยตอบนะ” แล้วหยุดรอ':'วันนี้ยังไม่ควรส่งข้อความซ้ำ ให้เว้นพื้นที่ก่อนแล้วค่อยถามหนึ่งเรื่องที่ชัดเจน';r.avoid='อย่าส่งข้อความทดสอบใจ ประชด หรือถามหลายคำถามติดกัน'}else if(q.id==='love_thinking'){r.verdict='เขายังนึกถึงคุณเป็นช่วง ๆ แต่ความคิดถึงยังไม่เท่ากับความพร้อมที่จะกลับมา';r.summary='พลังความสัมพันธ์ยังไม่ขาด แต่สิ่งที่ติดอยู่คือความลังเลและรูปแบบเดิมที่ยังไม่ได้แก้ การเฝ้าดูว่าเขาคิดถึงไหมจะให้คำตอบน้อยกว่าการดูว่าเขาลงมือสม่ำเสมอหรือไม่';r.action='ถ้ามีการติดต่อ ให้คุยเรื่องปัจจุบันสั้น ๆ ก่อน อย่ารีบถามสถานะหรือรื้อเรื่องเก่า';r.avoid='อย่าตีความการดูสตอรี่ ไลก์ หรืออ่านข้อความว่าเป็นคำสัญญาว่าจะกลับมา'}else if(q.id==='love_reconcile'){r.verdict='ยังมีช่องให้กลับมาคุย แต่ยังไม่ใช่จังหวะกลับไปเหมือนเดิมทันที';r.summary='โอกาสอยู่ที่การเริ่มบทสนทนาใหม่โดยไม่ใช้แรงกดดันเดิม หากกลับมาเพราะคิดถึงอย่างเดียว ปัญหาเดิมมีแนวโน้มวนซ้ำ';r.action='เริ่มจากเรื่องที่เป็นกลางและสังเกตความสม่ำเสมอ 2–3 ครั้ง ก่อนถามเรื่องความสัมพันธ์';r.avoid='อย่าเริ่มด้วยคำถามว่า “จะกลับมาไหม” หรือขอคำตอบในข้อความเดียว'}else if(q.id==='love_message'){var send=!cautious&&flow.lastContact!=='today';r.verdict=send?'ทักได้ แต่ควรเป็นข้อความสั้นที่ไม่บังคับให้อีกฝ่ายตอบทันที':'วันนี้ยังไม่ควรทักซ้ำ เพราะน้ำเสียงอาจออกมาแรงกว่าที่ตั้งใจ';r.summary='คำถามนี้ไม่ได้ตัดสินจากว่าใครควรเป็นฝ่ายเริ่ม แต่ดูว่าข้อความจะเปิดทางหรือเพิ่มแรงกดดัน จังหวะของคุณวันนี้เหมาะกับความชัดแบบสั้น ๆ';r.action=send?'ใช้ข้อความเดียว เช่น “ช่วงนี้เป็นยังไงบ้าง ถ้าว่างค่อยตอบนะ” แล้วไม่ส่งเพิ่ม':'เขียนข้อความไว้ก่อน แต่รอให้ใจนิ่งแล้วอ่านอีกครั้งในช่วงที่เหมาะ';r.avoid='อย่าถามว่า “ทำไมไม่ตอบ” และอย่าส่งข้อความหลายช่องทางพร้อมกัน'}else if(q.id==='work_change'){r.verdict='มีจังหวะให้เริ่มขยับงาน แต่ยังไม่ใช่จังหวะลาออกทันที';r.summary='ธาตุวันนี้สนับสนุนการเก็บข้อมูลและทดลองทางเลือกมากกว่าการตัดสินใจจากความอึดอัด คุณควรสร้างหลักฐานว่าทางใหม่ดีกว่า ก่อนตัดทางเดิม';r.action='อัปเดตประวัติ สมัครหรือคุยกับหนึ่งตำแหน่ง และเช็กเงินสำรองก่อนกำหนดวันลาออก';r.avoid='อย่าลาออกเพียงเพราะวันนี้เหนื่อย หรือรับข้อเสนอโดยยังไม่เห็นขอบเขตงานทั้งหมด'}else{r.verdict='จังหวะเงินจะดีขึ้นจากการจัดระบบและรายได้ที่ควบคุมได้ มากกว่าโชคลอย';r.summary='วันนี้เหมาะกับการเห็นเงินที่รั่วและโอกาสเล็กที่ทำซ้ำได้ การรอเงินก้อนเดียวจะทำให้รู้สึกช้ากว่าการเริ่มแก้หนึ่งจุดทันที';r.action='เช็กรายจ่ายประจำหนึ่งรายการ แล้วลงมือกับช่องทางรายได้หนึ่งอย่างภายในวันนี้';r.avoid='อย่าซื้อเพราะกลัวพลาด และอย่าตัดสินใจจากคำว่า “กำไรเร็ว”'}r.timing=nativeMoment();r.factor=e;r.feedback=feedbackAdjustment();return r}
function contextPayload(result){var q=currentQuestion();return{questionId:q.id,question:q.title,focus:q.focus,name:flow.name,birth:flow.birth,time:flow.time,mood:flow.mood,relation:flow.relation,lastContact:flow.lastContact,consent:flow.consent,result:result,updatedAt:new Date().toISOString(),date:bangkokDate()}}
function overlay(){var el=document.getElementById('tlQuestionFlow');if(el)return el;el=document.createElement('div');el.id='tlQuestionFlow';el.className='tl-flow';el.setAttribute('role','dialog');el.setAttribute('aria-modal','true');el.innerHTML='<div class="tl-flow-shell"><div class="tl-flow-top"><div class="tl-flow-brand"><div class="tl-flow-logo">☾</div><div><b>ดวงวันดี</b><small>ดวงจากวันเกิดแบบเกาหลี</small></div></div><button type="button" class="tl-flow-close" id="tlFlowClose" aria-label="ปิด">×</button></div><div class="tl-flow-progress" id="tlProgress"><i></i><i></i><i></i></div><div id="tlFlowBody"></div></div>';document.body.appendChild(el);document.getElementById('tlFlowClose').onclick=function(){if(!firstRun)closeFlow()};return el}
function progress(step){var dots=overlay().querySelectorAll('#tlProgress i');for(var i=0;i<dots.length;i++)dots[i].classList.toggle('on',i<step);var close=document.getElementById('tlFlowClose');if(close)close.style.visibility=firstRun?'hidden':'visible'}
function openFlow(mode){overlay().hidden=false;document.body.style.overflow='hidden';if(mode==='result'&&flow.result){renderResult();return}flow.step=1;renderQuestions()}
function closeFlow(){overlay().hidden=true;document.body.style.overflow='';document.documentElement.classList.remove('tl-first-run-pending')}
function renderQuestions(){progress(1);var body=document.getElementById('tlFlowBody');body.innerHTML='<span class="tl-flow-kicker">เริ่มจากเรื่องที่ค้างอยู่ในใจที่สุด</span><h1>วันนี้คุณอยากได้คำตอบเรื่องไหน?</h1><p class="tl-flow-lead">เลือกเพียงหนึ่งคำถามก่อน เราจะตอบให้ลึก แล้วค่อยเปิดดวงครบทั้งความรัก งาน เงิน และตัวคุณ</p><div class="tl-question-grid">'+questions.map(function(q){return'<button type="button" class="tl-question" data-question="'+q.id+'"><span class="tl-question-icon">'+q.icon+'</span><span><b>'+q.title+'</b><small>'+q.hint+'</small></span><em>›</em></button>'}).join('')+'</div><p class="tl-method-note">ใช้แนวคิดธาตุทั้งห้าจากวันเกิดแบบเกาหลีเพื่อความบันเทิงและทบทวนตนเอง</p>';body.querySelectorAll('[data-question]').forEach(function(btn){btn.onclick=function(){flow.question=questionById(btn.getAttribute('data-question'));flow.step=2;renderDetails()}})}
function choiceButtons(items,selected,attr){return items.map(function(item){return'<button type="button" class="tl-choice '+(selected===item[0]?'on':'')+'" data-'+attr+'="'+item[0]+'">'+item[1]+'</button>'}).join('')}
function renderDetails(){progress(2);var q=currentQuestion(),love=q.focus==='love',today=bangkokDate();var body=document.getElementById('tlFlowBody');body.innerHTML='<span class="tl-flow-kicker">คำถามของคุณ</span><h1>'+esc(q.title)+'</h1><p class="tl-flow-lead">ใส่ข้อมูลเท่าที่จำเป็นเพื่อเชื่อมคำถามนี้กับจังหวะจากวันเกิดของคุณ</p><div class="tl-flow-card"><div class="tl-field"><label>วันเกิด</label><input id="tlBirth" type="date" min="1940-01-01" max="'+today+'" value="'+esc(flow.birth)+'"></div><div class="tl-field"><label>วันนี้คุณรู้สึกแบบไหน?</label><div class="tl-choice-grid mood" id="tlMood">'+choiceButtons([['anxious','กังวล'],['unsure','ลังเล'],['tired','เหนื่อย'],['ready','พร้อมลุย']],flow.mood,'mood')+'</div></div>'+(love?'<div class="tl-field"><label>ความสัมพันธ์ตอนนี้</label><div class="tl-choice-grid" id="tlRelation">'+choiceButtons([['talking','คนคุย'],['partner','แฟน'],['ex','แฟนเก่า'],['crush','แอบชอบ'],['unsure','ไม่แน่ใจ']],flow.relation,'relation')+'</div></div><div class="tl-field"><label>ติดต่อกันล่าสุดเมื่อไร?</label><select id="tlLast"><option value="today">วันนี้</option><option value="days3">1–3 วัน</option><option value="week">4–7 วัน</option><option value="long">มากกว่า 1 สัปดาห์</option><option value="never">ยังไม่เคยคุย</option></select></div>':'')+'<details class="tl-optional"><summary>ข้อมูลเพิ่มเติม (ไม่บังคับ)</summary><div class="tl-field"><label>ชื่อเล่น <small>เว้นว่างได้</small></label><input id="tlName" maxlength="24" value="'+esc(flow.name)+'" placeholder="ไม่ระบุชื่อ"></div><div class="tl-field"><label>เวลาเกิด <small>ไม่ทราบก็เว้นได้</small></label><input id="tlTime" type="time" value="'+esc(flow.time)+'"></div></details><label class="tl-consent"><input id="tlConsent" type="checkbox" '+(flow.consent?'checked':'')+'><span>ส่งข้อมูลเริ่มต้นแบบไม่ผูกกับบัญชีให้ผู้ดูแล เพื่อวัดการเริ่มดูดวงและปรับปรุงบริการ คุณสามารถยกเลิกได้</span></label><div class="tl-error" id="tlError"></div><div class="tl-actions"><button type="button" class="tl-btn secondary" id="tlBack">ย้อนกลับ</button><button type="button" class="tl-btn primary" id="tlAnalyze">วิเคราะห์คำถามนี้</button></div></div>';if(love){var last=document.getElementById('tlLast');last.value=flow.lastContact}body.querySelectorAll('[data-mood]').forEach(function(b){b.onclick=function(){flow.mood=b.getAttribute('data-mood');body.querySelectorAll('[data-mood]').forEach(function(x){x.classList.toggle('on',x===b)})}});body.querySelectorAll('[data-relation]').forEach(function(b){b.onclick=function(){flow.relation=b.getAttribute('data-relation');body.querySelectorAll('[data-relation]').forEach(function(x){x.classList.toggle('on',x===b)})}});document.getElementById('tlBack').onclick=renderQuestions;document.getElementById('tlAnalyze').onclick=startAnalysis}
function collectDetails(){var birth=document.getElementById('tlBirth');flow.birth=birth?birth.value:'';var name=document.getElementById('tlName'),time=document.getElementById('tlTime'),consent=document.getElementById('tlConsent'),last=document.getElementById('tlLast');flow.name=name?name.value.trim():flow.name;flow.time=time?time.value:flow.time;flow.consent=consent?consent.checked:true;if(last)flow.lastContact=last.value;var err=document.getElementById('tlError');if(!flow.birth){err.textContent='กรุณาเลือกวันเกิดก่อนเปิดคำตอบ';birth&&birth.focus();return false}var d=new Date(flow.birth+'T00:00:00'),now=new Date();if(isNaN(d.getTime())||d>now){err.textContent='วันเกิดไม่ถูกต้อง กรุณาตรวจอีกครั้ง';return false}err.textContent='';return true}
function startAnalysis(){if(!collectDetails())return;progress(3);var body=document.getElementById('tlFlowBody');body.innerHTML='<div class="tl-loading"><div class="tl-orbit"></div><h2>กำลังอ่านคำถามของคุณ</h2><p id="tlLoadingText">กำลังอ่านพลังพื้นฐานจากวันเกิด</p><div class="tl-loading-steps"><div class="on"><i></i>อ่านธาตุพื้นฐานจากวันเกิด</div><div><i></i>เทียบกับจังหวะของวันนี้</div><div><i></i>สรุปคำตอบและสิ่งที่ควรทำ</div></div></div>';formSubmitObserved=false;triggerNativeReading();var rows=body.querySelectorAll('.tl-loading-steps div'),texts=['กำลังอ่านพลังพื้นฐานจากวันเกิด','กำลังเทียบกับจังหวะของคำถาม','กำลังสรุปสิ่งที่ควรทำวันนี้'];var index=0;var timer=setInterval(function(){index++;if(index<rows.length){rows[index].classList.add('on');document.getElementById('tlLoadingText').textContent=texts[index]}},650);setTimeout(function(){clearInterval(timer);flow.result=makeResult();writeJson(CONTEXT_KEY,contextPayload(flow.result));renderResult()},2250)}
function revealHtml(result,type){if(type==='reason')return'<div class="tl-factor-grid"><div class="tl-factor"><span>ธาตุพื้นฐาน</span><b>'+result.factor.base.icon+' '+result.factor.base.th+'</b></div><div class="tl-factor"><span>จังหวะวันนี้</span><b>'+result.factor.day.icon+' '+result.factor.day.th+'</b></div><div class="tl-factor"><span>อารมณ์วันนี้</span><b>'+({anxious:'กังวล',unsure:'ลังเล',tired:'เหนื่อย',ready:'พร้อมลุย'}[flow.mood]||'ลังเล')+'</b></div></div><p>'+result.factor.relation+'</p><p>จึงนำมารวมกับคำถาม “'+esc(currentQuestion().title)+'” และบริบทที่คุณเลือก ไม่ได้ใช้ข้อความเดียวกับทุกคน</p>';return'<p><b>ช่วงที่เหมาะ:</b> '+esc(result.timing)+'</p><div class="tl-do-dont"><div><span>ควรทำ</span><p>'+esc(result.action)+'</p></div><div><span>ควรเลี่ยง</span><p>'+esc(result.avoid)+'</p></div></div>'}
