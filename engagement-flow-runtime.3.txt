function renderResult(){progress(3);var r=flow.result||makeResult(),q=currentQuestion();flow.result=r;writeJson(CONTEXT_KEY,contextPayload(r));var body=document.getElementById('tlFlowBody');body.innerHTML='<span class="tl-flow-kicker">คำตอบแรกจากคำถามของคุณ</span><div class="tl-result-question">'+q.icon+' '+esc(q.title)+'</div><div class="tl-verdict"><span>คำตอบโดยสรุป</span><h2>'+esc(r.verdict)+'</h2><p>'+esc(r.summary)+'</p>'+(r.feedback?'<div class="tl-feedback-note">'+esc(r.feedback)+'</div>':'')+'</div><div class="tl-reveal"><button type="button" data-reveal="reason">ทำไมจึงได้คำตอบนี้? <b>＋</b></button><div class="tl-reveal-content">'+revealHtml(r,'reason')+'</div></div><div class="tl-reveal"><button type="button" data-reveal="action">ดูจังหวะและสิ่งที่ควรทำ <b>＋</b></button><div class="tl-reveal-content">'+revealHtml(r,'action')+'</div></div><div class="tl-return-note">คำตอบรายวันของคำถามนี้จะอัปเดตหลังเที่ยงคืน กลับมาบันทึกว่าเกิดอะไรขึ้น เพื่อให้คำแนะนำวันถัดไปเปลี่ยนตามผลจริง</div><button type="button" class="tl-btn primary full" id="tlFullReading">ดูดวงครบทุกด้านต่อ</button><button type="button" class="tl-btn secondary full" id="tlAnother">เลือกคำถามอื่น</button><p class="tl-method-note">เป็นการอ่านจังหวะแบบย่อจากวันเกิดและธาตุทั้งห้า ไม่ใช่การรับรองเหตุการณ์ในอนาคต</p>';body.querySelectorAll('[data-reveal]').forEach(function(btn){btn.onclick=function(){var box=btn.parentElement;box.classList.toggle('open');btn.querySelector('b').textContent=box.classList.contains('open')?'−':'＋'}});document.getElementById('tlFullReading').onclick=finishFlow;document.getElementById('tlAnother').onclick=renderQuestions}
function finishFlow(){localStorage.setItem(COMPLETE_KEY,'1');firstRun=false;visitorType='returning';document.documentElement.classList.remove('tl-first-run-pending');closeNativeModal();closeFlow();decorateHome();setTimeout(function(){window.scrollTo({top:0,behavior:'smooth'})},30)}
function closeNativeModal(){document.querySelectorAll('.modal.on,.modal-backdrop.open,.modal-backdrop.on').forEach(function(m){m.classList.remove('on','open')})}
function decorateHome(){var ctx=readJson(CONTEXT_KEY);if(!ctx||!ctx.result)return;var home=document.querySelector('#home,#screen-home');if(!home)return;var old=document.getElementById('tlHomeFocus');if(old)old.remove();var card=document.createElement('section');card.id='tlHomeFocus';card.className='tl-home-focus';card.innerHTML='<span>คำถามที่คุณเลือกวันนี้</span><h2>'+esc(ctx.question)+'</h2><p>'+esc(ctx.result.verdict)+'</p>'+(ctx.result.feedback?'<div class="tl-feedback-note">'+esc(ctx.result.feedback)+'</div>':'')+'<div class="tl-home-focus-actions"><button type="button" class="again">ดูคำตอบเป็นขั้นตอน</button><button type="button" class="change">เปลี่ยนคำถาม</button></div>';var hero=home.querySelector('.hero');home.insertBefore(card,hero||home.firstChild);var label=document.createElement('div');label.className='tl-expand-label';label.textContent='ต่อจากคำถามนี้ · ดูความรัก งาน เงิน และตัวคุณครบทุกด้านจากวันเกิดแบบเกาหลี';card.insertAdjacentElement('afterend',label);card.querySelector('.again').onclick=function(){flow.question=questionById(ctx.questionId);flow.result=ctx.result;openFlow('result')};card.querySelector('.change').onclick=function(){openFlow('questions')};replaceRefresh();renameCategoryHeading();decorateCheckin()}
function replaceRefresh(){var candidates=[];var ids=['refreshFortuneBtn','refreshBtn','newReadingBtn'];ids.forEach(function(id){var el=document.getElementById(id);if(el)candidates.push(el)});document.querySelectorAll('button').forEach(function(b){if((b.textContent||'').trim()==='ดูใหม่')candidates.push(b)});candidates.forEach(function(btn){btn.textContent='เลือกคำถามใหม่';btn.addEventListener('click',function(e){e.preventDefault();e.stopImmediatePropagation();openFlow('questions')},true)})}
function renameCategoryHeading(){document.querySelectorAll('.head,.section-head').forEach(function(head){var h=head.querySelector('h2');if(!h)return;var text=h.textContent||'';if(text.indexOf('เรื่องที่')>=0&&text.indexOf('วันนี้')>=0){h.textContent='ดูต่อจากคำถามนี้';var p=head.querySelector('p');if(p)p.textContent='เปิดดูความรัก งาน เงิน และตัวคุณให้ครบได้ด้านล่าง'}})}
function decorateCheckin(){var check=document.querySelector('.check,.checkin,#checkinCard');if(!check)return;if(!check.querySelector('.tl-check-impact')){var note=document.createElement('div');note.className='tl-check-impact';note.textContent='คำตอบที่เลือกจะเปลี่ยนคำแนะนำของคำถามนี้ในวันถัดไป';check.appendChild(note)}}
function installCheckin(){document.addEventListener('click',function(e){var b=e.target.closest('[data-r],[data-result]');if(!b)return;var raw=b.getAttribute('data-r')||b.getAttribute('data-result');var result=raw==='good'?'good':raw==='bad'?'bad':'same';var ctx=readJson(CONTEXT_KEY);writeJson(CHECK_KEY,{date:bangkokDate(),result:result,questionId:ctx&&ctx.questionId||'',at:new Date().toISOString()});setTimeout(decorateCheckin,50)},false)}
function init(){installFetchHook();retryPending();installCheckin();overlay();if(firstRun){openFlow('questions')}else{document.documentElement.classList.remove('tl-first-run-pending');overlay().hidden=true;decorateHome();replaceRefresh();renameCategoryHeading();decorateCheckin()}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();