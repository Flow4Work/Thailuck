{
  "name": "thailuck",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html about.html privacy.html terms.html dist/ && cp -R payload dist/payload"
  }
}
